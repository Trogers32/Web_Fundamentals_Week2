USE TWITTER

SELECT * FROM USERS

INSERT INTO USERS (FIRST_NAME, LAST_NAME, HANDLE, BIRTHDAY, CREATED_AT, UPDATED_AT)

VALUES ('Tim', 'Rogers', 'ocelot', '1990-11-13', NOW(), NOW())

UPDATE USERS

SET LAST_NAME = 'GARBAGE'

WHERE ID = 1;

DELETE FROM USERS WHERE id = 6;